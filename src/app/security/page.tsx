import { FinalCta } from '@/components/site';

export const metadata = { title: 'Security and Trust | ProcureIQ', description: 'ProcureIQ trust posture for responsible AI, permissions, workspace boundaries, and auditability.' };
const sections = [['01', 'Human control', 'AI assistance stays draft-based. Purchasing action requires an authorized person.'], ['02', 'Reviewable context', 'Teams can see the context behind important supplier decisions before approval.'], ['03', 'Workspace boundaries', 'Supplier information, requests, decisions, and drafts are scoped to the authenticated workspace.'], ['04', 'Document handling', 'Sensitive supplier materials are handled through server-side storage controls.'], ['05', 'Auditability', 'Important supplier, quote, approval, and export activity can be recorded for operating visibility.'], ['06', 'Responsible AI posture', 'Uncertainty remains visible, and customer data is not used for model training without explicit consent.'], ['07', 'Admin readiness', 'Role and admin foundations support controlled access as teams expand.']];
export default function SecurityPage() { return <main><section className="page-hero section-shell compact"><p className="eyebrow">Security and trust</p><h1>Designed for sensitive supplier decisions.</h1><p>ProcureIQ emphasizes human control, workspace boundaries, permissions, auditability, and responsible AI without claiming certifications that are not yet in place.</p></section><section className="section-shell module-list">{sections.map(([num, title, body]) => <article className="numbered-module" key={title}><span>{num}</span><div><h2>{title}</h2><p>{body}</p></div></article>)}</section><FinalCta /></main>; }
