import Link from 'next/link';

export const metadata = { title: 'Demo Workflow | ProcureIQ', description: 'Explore a polished sample ProcureIQ product tour with demo supplier data.' };
const suppliers = [['Northline Metals', 'Complete response', 'Selected sample'], ['Atlas Components', 'Cost advantage', 'Needs context'], ['Kinetic Supply', 'Fastest timing', 'Review terms']];
const tour = [['Overview', 'Apex Industrial Works', 'Conveyor Guard Bracket Package'], ['Supplier view', 'Three responses', 'Context visible'], ['Decision view', 'Draft recommendation', 'Human review required'], ['Draft view', 'Action ready', 'Not sent automatically']];
export default function DemoWorkflowPage() { return <main><section className="page-hero section-shell demo-hero"><div><p className="eyebrow">Demo/sample data</p><h1>Experience the product rhythm without a sales call.</h1><p>This public tour uses sample data for Apex Industrial Works. It shows the feel of ProcureIQ while keeping the internal intelligence layer discreet.</p><div className="hero-actions"><Link className="button primary" href="/signup">Create Account</Link><Link className="button secondary" href="/demo">Book Demo</Link></div></div><div className="demo-console"><div className="demo-tabs">{tour.map(([tab]) => <button key={tab}>{tab}</button>)}</div><div className="demo-stage">{tour.map(([tab, title, body]) => <article key={tab}><span>{tab}</span><strong>{title}</strong><p>{body}</p></article>)}</div></div></section><section className="section-shell sample-tour"><div className="section-kicker"><p className="eyebrow">Sample workspace</p><h2>Conveyor Guard Bracket Package.</h2><p>Demo data is intentionally summarized to show product quality without revealing proprietary mechanics.</p></div><div className="tour-grid">{suppliers.map(([name, title, note]) => <article key={name}><span>{name}</span><h3>{title}</h3><p>{note}</p></article>)}</div><div className="demo-decision-panel"><span>Draft recommendation</span><strong>Northline Metals</strong><p>Selected in this sample because the context is ready for review. A person still approves any purchasing action.</p></div></section></main>; }
