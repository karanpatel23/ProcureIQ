import Link from 'next/link';
import { DecisionMatrix, EvidencePin, PODraftPreview, RFQPacket, SupplierMemoryCard } from '@/components/site';

export const metadata = { title: 'Demo Workflow | ProcureIQ', description: 'Explore a polished sample ProcureIQ product tour with demo supplier data.' };
const suppliers = [['Northline Metals', 'Complete response', '12 day lead', 'Sample'], ['Atlas Components', 'Cost advantage', 'Freight exception', 'Sample'], ['Kinetic Supply', 'Fastest timing', 'Terms need review', 'Sample']];
const fields = [['Supplier context', 'Ready for review', 'Demo evidence', 'Visible'], ['Commercial terms', 'Partially aligned', 'Demo evidence', 'Needs review'], ['Timing', 'Decision-ready', 'Demo evidence', 'Visible'], ['Approval status', 'Human review required', 'Demo policy', 'Locked']];
export default function DemoWorkflowPage() { return <main><section className="page-hero section-shell"><div><p className="eyebrow">Demo/sample data</p><h1>A polished preview of procurement intelligence.</h1><p>This public tour uses sample data for Apex Industrial Works and the Conveyor Guard Bracket Package. It shows the product feel without exposing ProcureIQ’s internal playbook.</p><div className="hero-actions"><Link className="button primary" href="/signup">Create Account</Link><Link className="button secondary" href="/demo">Book Demo</Link></div></div><RFQPacket /></section><section className="section-shell demo-workflow"><div className="section-heading-block"><p className="eyebrow">Apex Industrial Works · sample data</p><h2>Conveyor Guard Bracket Package.</h2><p>Sample supplier activity is summarized at a buyer-friendly level so visitors can experience the interface without seeing proprietary workflow logic.</p></div><div className="quote-card-grid">{suppliers.map(([name, position, note, label]) => <article key={name}><span>{name}</span><strong>{position}</strong><p>{note}</p><EvidencePin label="demo data" confidence={label} /></article>)}</div><section className="review-table"><h2>Guided review snapshot</h2><table><thead><tr><th>Area</th><th>Status</th><th>Context</th><th>Review</th></tr></thead><tbody>{fields.map((row) => <tr key={row[0]}>{row.map((cell) => <td key={cell}>{cell}</td>)}</tr>)}</tbody></table></section><div className="risk-board"><span className="risk-badge medium">Freight exception visible</span><span className="risk-badge high">Terms require review</span><span className="risk-badge medium">Approval remains manual</span></div><DecisionMatrix /><section className="selected-supplier"><p className="eyebrow">Selected supplier · sample</p><h2>Northline Metals selected with a decision note.</h2><p>The sample decision remains a draft recommendation until a person reviews and approves the next purchasing action.</p></section><PODraftPreview /><SupplierMemoryCard /></section></main>; }
