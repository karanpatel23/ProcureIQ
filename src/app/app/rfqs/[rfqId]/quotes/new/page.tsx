import { QuoteUploadForm } from '@/components/quotes/QuoteUploadForm';
import { requirePageWorkspace } from '@/lib/server/auth';
import { readDb } from '@/lib/server/db';
import { notFound } from 'next/navigation';
export const metadata = { title: 'Add supplier quote | ProcureIQ' };
export default async function AddQuotePage({ params }: { params: Promise<{ rfqId: string }> }) { const { workspace } = await requirePageWorkspace(); const { rfqId } = await params; const db = await readDb({ workspaceId: workspace.id }); const rfq = db.rfqs.find((item) => item.id === rfqId && item.workspaceId === workspace.id); if (!rfq) notFound(); const suppliers = db.suppliers.filter((supplier) => supplier.workspaceId === workspace.id && rfq.supplierIds.includes(supplier.id) && !supplier.archivedAt); return <main><section className="app-shell"><p className="eyebrow">Add supplier quote</p><h1>Upload or paste a quote for review.</h1><p>ProcureIQ extracts draft quote fields with confidence scores from pasted or text quotes, stores your original attachment, and sends you to a review screen. Every number stays traceable to its source.</p><QuoteUploadForm rfqId={rfq.id} suppliers={suppliers} /></section></main>; }
